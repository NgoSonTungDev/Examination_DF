import apiSlice from "./api/api_slice";
import authSlice from "./auth/auth_slice";
import projectSlice from "./project/project_slice";

export const reducer = {
  apiSlice,
  authSlice,
  projectSlice,
};
